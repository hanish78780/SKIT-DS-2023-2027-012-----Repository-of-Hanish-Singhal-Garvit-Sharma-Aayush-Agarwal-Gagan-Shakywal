import colors from './colors';
import shadows from './shadows';
import { spacing, borderRadius } from './spacing';
import typography from './typography';

export { colors, shadows, spacing, borderRadius, typography };

export default {
  colors,
  shadows,
  spacing,
  borderRadius,
  typography,
};
